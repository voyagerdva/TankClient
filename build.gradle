plugins {
    id 'java'
}

group 'ru.nn'
version '0.0.1'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.9.2'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.9.2'
    implementation 'org.postgresql:postgresql:42.6.0' // Подключаем зависимость
//    driver group: 'org.postgresql', name: 'postgresql', version: '42.6.0'
//    compile group: 'org.postgresql', name: 'postgresql', version: '42.6.0'
}

test {
    useJUnitPlatform()
}